spring:
  application:
    name: employee-activity-service # Change this for each service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true

server:
  port: 8083 # Let the server choose a random available port